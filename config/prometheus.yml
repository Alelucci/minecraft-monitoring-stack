# Prometheus configuration file to read the exporter's data

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'minecraft-exporter'
    static_configs:
      - targets: ['exporter:8080']

